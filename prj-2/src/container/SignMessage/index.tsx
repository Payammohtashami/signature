import React from 'react';
import styles from "./SignMessage.module.sass";

const Login:React.FC = () => {
    return (
        <div>
            <h1>Login Page</h1>
        </div>
    );
};

export default Login;